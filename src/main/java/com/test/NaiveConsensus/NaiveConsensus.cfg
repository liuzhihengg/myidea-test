\* 1. 声明入口规约 (Specification)
SPECIFICATION Spec

\* 2. 运行时一致性检查 (Invariants)
\* 这是分布式系统的生命线：绝对不允许集群内部出现“认知分裂”。
INVARIANT TypeOK
INVARIANT Consistency

\* 3. 搜索策略限制 (由于消息集合 msgs 会不断增大)
\* 对于 3 个节点的简单模型，默认配置即可。
\* 如果发生状态爆炸，可以取消下面这一行的注释来限制搜索深度。
\* CONSTRAINT StateConstraint