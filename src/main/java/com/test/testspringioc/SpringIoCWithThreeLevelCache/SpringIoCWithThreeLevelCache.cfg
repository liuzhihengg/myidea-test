\* 1. 指定入口规约
SPECIFICATION Spec

\* 2. 运行时一致性检查 (这是本次验证的杀手锏)
\* 一旦 Bean B 拿到的 A_RAW 与 A 最终生成的 A_PROXY 不符，这里就会报警。
INVARIANT TypeOK
INVARIANT Consistency

\* 3. 活性属性检查 (验证是否依然没有死锁)
PROPERTY AllInitialized

\* 4. 数据限制 (由于引入了 exportedValue 状态，空间会变大)
\* M4 Pro 48GB 依然可以秒杀，无需额外限制。